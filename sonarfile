pipeline{
    agent any
    stages{
        stage('sonar scan'){
            agent{
                image 'maven'
                args '-v /root/.m2:/root/'.m2
            }
            steps{
                script{
                    sh "mvn clean install"

                }
            
            }

        }
    }
}